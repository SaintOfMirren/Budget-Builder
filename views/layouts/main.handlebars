<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Budget Builder</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" type="image/x-icon" href="https://cdn-icons-png.flaticon.com/512/1211/1211547.png" />
</head>

<body>
  <div class="flex-column justify-space-around mask rgba-gradient bodyContainer">
    <header class="display-flex justify-space-between align-center p-2">
      <h1 class="home__title">
        <span role="img" aria-label="money">💸</span>
        Monthly Budget Planner
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/home" class="text-warning">Home</a> |
        <a href='/expense' id="expense" class="text-warning">Expense</a> |
        
        <button class="no-button" id="logout"><div class="text-danger">Logout</div></button>
        {{else}}
        <a href="/login" class="text-warning">Login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}      
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

</body>

</html>